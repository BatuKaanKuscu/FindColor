services:
  - type: web
    name: find-color
    runtime: static
    buildCommand: echo "No build required"
    staticPublishPath: web-pwa
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
